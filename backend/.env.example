# =============================================================================
# Backend environment configuration — TEMPLATE ONLY
# -----------------------------------------------------------------------------
# ⚠  This file is committed to git. NEVER put real credentials here.
#
# Setup:
#   1. Copy this file:    cp backend/.env.example backend/.env
#   2. Open backend/.env  and replace every "your_..." placeholder with the
#                         real value from your Hostinger hPanel → Databases.
#   3. Restart:           npm --prefix backend run dev
#
# backend/.env is gitignored — your real secrets stay on your machine only.
# If a real password ever lands in this file, rotate it in hPanel immediately.
# =============================================================================

# ---- Server ----
PORT=4000
NODE_ENV=development

# Comma-separated list of allowed origins for CORS.
# Use "*" only for local dev. In production, list your real frontend URLs.
CORS_ORIGIN=http://localhost:5173,http://localhost:3000

# ---- Database (MySQL / Hostinger) ----
# PLACEHOLDERS ONLY — put the real values in backend/.env, never here.
DB_HOST=your_db_host.hstgr.io
DB_PORT=3306
DB_NAME=your_db_name
DB_USER=your_db_user
DB_PASSWORD=your_db_password

# Connection pool tuning
DB_CONNECTION_LIMIT=10
DB_QUEUE_LIMIT=0
DB_CONNECT_TIMEOUT_MS=10000

# Set to "true" if your MySQL host requires SSL (most Hostinger shared hosts do NOT).
DB_SSL=false

# ---- Auth (JWT) ----
# REQUIRED. Use a long random string (32+ chars). Generate one with:
#   node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
JWT_SECRET=replace_me_with_a_long_random_string_at_least_32_chars
JWT_EXPIRES_IN=12h
